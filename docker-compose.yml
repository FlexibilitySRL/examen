version: '3.1'

services:

  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: password
    ports:
      - 3306:3306

# docker stack deploy -c stack.yml mysql O docker-compose -f stack.yml up
# TODO: agregar la app