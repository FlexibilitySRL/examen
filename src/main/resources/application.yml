spring:
  profiles: local
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:mysql://localhost:3306/examen?zeroDateTimeBehavior=convertToNull&createDatabaseIfNotExist=true
    username: root
    password: Tecso2019
  h2:
    console:
      enabled: true
