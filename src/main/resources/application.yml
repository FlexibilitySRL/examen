spring:
  #profiles: local
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    #database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    #datasource:
    #url: jdbc:mysql://127.0.0.1:3306/aramark?zeroDateTimeBehavior=convertToNull
    #username: root
    #password: password

---