spring:
  jackson:
    serialization:
      write-dates-as-timestamps: false
  jpa:
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
    show-sql: true
  datasource:
      url: jdbc:mysql://mysqldb:3306/challenge?allowPublicKeyRetrieval=true&useSSL=true
      username: root
      password: toor

server:
  port: 8080
  servlet:
    context-path: '/payments'
---
