spring:
  jackson:
    serialization:
      write-dates-as-timestamps: false
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
 
server:
  port: 8080
  servlet:
    context-path: '/payments'