spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:payments;DB_CLOSE_ON_EXIT=FALSE
    
    username: marca
    password: acme
  h2:
    console:
      enabled: true
      path: /h2-console
 
