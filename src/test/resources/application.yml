spring:
  profiles: test
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: jdbc:h2:~/db/transactions
    driverClassName: org.h2.Driver
    username: sa
    password: password
  h2:
    console:
      enable: true


---